# npm-specific: Use standard peer dependency resolution.
# Keep this file limited to npm-supported keys because Vercel invokes npm
# for commands like `npx turbo-ignore` even though the repo uses Bun.
legacy-peer-deps=false
