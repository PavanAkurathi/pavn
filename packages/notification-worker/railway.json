{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "RAILPACK",
        "installCommand": "bun install",
        "buildCommand": "bun run build --filter=@repo/notification-worker",
        "watchPatterns": [
            "packages/notification-worker/**",
            "packages/database/**",
            "packages/notifications/**",
            "pnpm-lock.yaml",
            "turbo.json"
        ]
    },
    "deploy": {
        "startCommand": "bunx turbo run start --filter=@repo/notification-worker",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}