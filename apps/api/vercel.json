{
    "buildCommand": "node build-vercel.mjs",
    "outputDirectory": ".",
    "installCommand": "npm install esbuild --save-dev",
    "framework": null,
    "functions": {
        "api/index.js": {
            "memory": 1024,
            "maxDuration": 30
        }
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ]
}
